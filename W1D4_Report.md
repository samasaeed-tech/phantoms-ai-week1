# W1D4: Dev Environment & Git Strategy

في التقرير ده بوضح اختياراتي التقنية لبيئة التطوير وإستراتيجية العمل مع التيم في مشروعنا.

---

## 1. اختيار بيئة التطوير (IDE Selection)

عملت مقارنة سريعة بين أشهُر الأدوات المتاحة:

* **VS Code:** خفيف وسريع جداً، وفيه Extensions بتسهل شغل البايثون وجيت، وبيدعم تشغيل ملفات السكريبت `.py` وملفات `.ipynb` في نفس المكان بسهولة.
* **PyCharm:** بيئة ممتازة للبايثون وبتساعد في الـ Debugging، بس تقيلة على الجهاز وتستهلك RAM عالية.
* **Google Colab:** ممتاز وسهل في التجارب السريعة وموديلات الـ AI عشان بيوفر GPU مجاني، بس مش أنسب حاجة لتنظيم مشروع كامل على الجهاز.

### القرار النهائي:
اختياري الأساسي للمشاريع هو **VS Code**، مع استخدام **Google Colab** للتجارب السريعة أو لو محتاجة قوة معالجة أعلى أثناء التجربة.

---

## 2. أهمية البيئات الافتراضية (Virtual Environments)

استخدام الـ Virtual Environment خطوة أساسية عشان نعزل مكتبات كل مشروع عن التاني.

* **ليه بنستخدمها؟** عشان نضمن إن كلنا شغالين بنفس إصدارات المكتبات بالضبط، ونمنع المشكلة الشهيرة بتاعت "الكود شغال عندي ومش شغال عندك".
* **طريقة العمل مع التيم:**
  1. بنعمل بيئة معزولة بالأمر: `python -m venv venv`
  2. بنفعل البيئة وننزل مكتبات المشروع من ملف متوحد: `pip install -r requirements.txt`
  3. لما بنضيف أي مكتبة جديدة بنحدث الملف بالأمر: `pip freeze > requirements.txt`

---

## 3. إستراتيجية الفروع في جيت (Git Branching Strategy)

عشان نضمن إن الكود الرئيسي شغال دايماً من غير مشاكل، هنعتمد على إستراتيجية **Feature Branching**:

* **الفرع الرئيسي (`main`):** هيكون للمراحل النهائية والكود المستقر الشغال تماماً.
* **فروع الميزات (`feature/*`):** أي حد هيشتغل على جزء جديد أو التاسك بتاعه بيعمل فرع خاص بيه (مثال: `feature/data-processing`).
* **مراجعة الكود (Pull Requests):** ممنوع الرفع المباشر على `main`. الشغل بيترفع على الفرع الخاص، وبنعمل Pull Request والمراجعة بتتم قبل ما نعمل Merge للـ `main`.
